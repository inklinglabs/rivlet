# Rivlet

Give any website its own Mac app. A lean, open source successor to Fluid,
built on WKWebView for macOS 26 and later.

Work happens on `dev`. `main` is the stable branch and only moves at
release time.

Design and scope live in `docs/specs/`.
